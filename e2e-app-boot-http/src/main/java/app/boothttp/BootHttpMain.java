package app.boothttp;

import one.xis.boot.XISBootApplication;
import one.xis.boot.XISBootRunner;

@XISBootApplication
public class BootHttpMain {

    public static void main(String[] args) {
        XISBootRunner.run(BootHttpMain.class, args);
    }
}
